<form method="post" action="{{ path('app_bitcoin_delete', {'id': bitcoin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bitcoin.id) }}">
    <button class="btn">Delete</button>
</form>
